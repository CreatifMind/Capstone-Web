# Project Rules for Capstone Web Workspace

## Git Branch Management & Sync Policy
- Whenever staging, committing, and pushing updates to `git main`, always synchronize all local branches with their corresponding remote tracking branches (`backend`, `backup-before-onnx-integration`, `dashboards`, `feature/browser-onnx-poc`, `feature/onnx-upload-integration`, `reference/purityloop-b2b-homepage`, `upload.html`, `webpage`).
